{{#> dotdungeon.panel class="roles" title="dotdungeon.actor.pc.panel.roles"}}
	{{#each system.roles}}
		<textarea
			name="system.roles.{{@key}}"
		>{{this}}</textarea>
	{{/each}}
{{/ dotdungeon.panel}}